# Copy to the mounted config volume as credentials.env (not the project root).
# Portainer: edit /app/config/credentials.env inside the volume — no quotes, no $$.
# Example path on the host bind: ./config/credentials.env
FUBO_USER=you@example.com
FUBO_PASS=your-password
